# Qui Piv posioy Syn ui Sin you `.vso` posioy is piv, h's h **sis wy** o syn you VS o onfiuions: ## **ho 1: iHub Wb Inf (on)** ### Sp 1: Opn You Piv posioy 1. o o: **hps://ihub.o/SiLIF/.vso** 2. Nvi o h fils you wn o syn ### Sp 2: opy Fils On by On Fo h fil you wn o syn: #### **sins.json** 1. li on `sins.json` in you posioy 2. li h **"w"** buon 3. Sl ll (l+) n opy (l+) 4. un: `pyhon - "ipo json; onn=inpu('Ps sins.json onn: '); json.los(onn); pin(' Vli JSON')"` 5. If vli, sv o `.vso/sins.json` #### **ss.json** 1. li on `ss.json` 2. li **"w"** 3. opy onn 4. Sv o `.vso/ss.json` #### **lunh.json** 1. li on `lunh.json` 2. li **"w"** 3. opy onn 4. Sv o `.vso/lunh.json` #### **xnsions.json** 1. li on `xnsions.json` 2. li **"w"** 3. opy onn 4. Sv o `.vso/xnsions.json` ## **ho 2: i lon wih uhniion** If you hv i onfiu wih you iHub nils: ```bash # lon o poy ioy i lon hps://ihub.o/SiLIF/.vso.i pvso # opy .vso ioy xopy pvso\.vso .vso / /I /Y # ln up i /S pvso ``` ## **ho 3: ownlo s ZIP** 1. o o you piv posioy: **hps://ihub.o/SiLIF/.vso** 2. li h n **"o"** buon 3. Sl **"ownlo ZIP"** 4. x h ZIP fil 5. opy h `.vso` fol o you un poj ## ️ **Sf in ons** f in h fils, us hs ons o sfly : ###  sins.json ```pyhon pyhon - " ipo json, shuil fo phlib ipo Ph # Bup xisin xisin = Ph('.vso/sins.json') if xisin.xiss(): shuil.opy2(xisin, xisin.wihsuffix('.bup')) # Lo boh fils wih opn('.vso/sins.json', '') s f: un = json.lo(f) wih opn('ownlosins.json', '') s f: nw = json.lo(f) #  (nw vlus ovi)  = {**un, **nw} # Sv  wih opn('.vso/sins.json', 'w') s f: json.up(, f, inn=2) pin(' sins.json  sussfully') " ``` ###  ss.json ```pyhon pyhon - " ipo json fo phlib ipo Ph # Lo fils wih opn('.vso/ss.json', '') s f: un = json.lo(f) wih opn('ownloss.json', '') s f: nw = json.lo(f) #  ss ys unlbls = {s.('lbl') fo s in un.('ss', [])} fo s in nw.('ss', []): if s.('lbl') no in unlbls: if 'ss' no in un: un['ss'] = [] un['ss'].ppn(s) # Sv wih opn('.vso/ss.json', 'w') s f: json.up(un, f, inn=2) pin(' ss.json  sussfully') " ``` ## **Vliion** f synin, vli you onfiuion: ```bash pyhon - " ipo json fo phlib ipo Ph vsoi = Ph('.vso') fils = ['sins.json', 'ss.json', 'lunh.json', 'xnsions.json'] fo filn in fils: filph = vsoi / filn if filph.xiss(): y: wih opn(filph, '') s f: json.lo(f) pin(f' {filn}: Vli') xp json.JSONoo s : pin(f' {filn}: Invli JSON - {}') ls: pin(f' ️ {filn}: No foun') " ``` ## **Qui ons fo You un Sup** ```bash #  .vso ioy if n i .vso 2>nul # Vli xisin onfiuion pyhon - "ipo json; pin('un sins ys:', lis(json.lo(opn('.vso/sins.json')).ys()))" # Insll xnsions fo xnsions.json pyhon - " ipo json y: wih opn('.vso/xnsions.json', '') s f:  = json.lo(f) fo x in .('onions', []): pin(f'o --insll-xnsion {x}') xp FilNoFouno: pin('No xnsions.json foun') " ``` ## **f Syn** 1. **s VS o**: los n opn VS o 2. **Opn Wosp**: Opn you `.o-wosp` fil 3. **s bu**: Pss F5 o s bu onfiuions 4. **un ss**: l+Shif+P → "ss: un s" 5. **Insll xnsions**: Us h ons n bov --- ** ol**:  you piv .vso posioy onfiuions sfly ino his poj wihou i onflis!